# 阶段 0–1 现场验收报告

执行日期：2026-08-18（Asia/Shanghai）

## 验收范围

- 项目方确认 DPI 范围为 100% 和 125%，不要求 150%。
- 调整后的质量门为至少 200 个已标注事件，100%/125% 各至少 80 个。
- 其余指标保持不变：抓帧成功率 ≥99%、最大网格误差 ≤10%、变化点召回率 ≥99%、静止误报率 ≤0.5%。

## 现场环境

- Windows 显示器物理分辨率：1920×1080。
- 100%：Electron `scaleFactor = 1`，逻辑分辨率 1920×1080。
- 125%：Electron `scaleFactor = 1.25`，逻辑分辨率 1536×864。
- CPU：Intel Core i5-9400F，6 核 6 线程；Pikafish 使用 BMI2 版本。
- QQ 游戏大厅：`5.94.58969.0`。
- 客户端进程：`QQChess2021.exe`，窗口标题“天天象棋”。

## 捕获路径与现场结论

- 可见的“天天象棋”窗口源能够被唯一枚举，但 Electron 窗口缩略图仍为全黑；这不是窗口最小化导致。
- Electron 整屏来源能够稳定取得完整棋盘，因此正式应用已加入 `window + screen` 来源枚举；窗口源黑屏时选择“整个屏幕”，再点击棋盘左上/右下交叉点校准。
- 100% 校准：整屏缩略图 1600×900；棋盘裁剪约 `(865,148,532,600)`，网格约 `(876,158)–(1391,739)`。
- 125% 首次校准：棋盘裁剪约 `(765,133,584,646)`，网格约 `(805,168)–(1310,736)`。
- 客户端移动后，旧 125% Profile 产生的 15 张偏移样本被识别并剔除；重新校准为裁剪 `(688,117,582,646)`、网格 `(727,151)–(1232,719)` 后复测通过。
- 该偏移批次仅作为 Profile 失效/重新校准证据，不进入合格数据集。

## 捕获质量结果

- 合格事件总数：200。
- DPI 100%：96（80 个静止、16 个经连续稳定帧视觉复核的走子/高亮/动画事件）。
- DPI 125%：104（95 个静止、9 个经连续稳定帧视觉复核的走子/吃子/高亮/动画事件）。
- 训练/留出事件：89 / 111；同一采集会话与 DPI 始终位于同一分组。
- 训练集 P99.5 静止阈值：`0`。
- 跨 DPI 高阈值：`0.007593780598893086`。现场数据发现原算法把高阈值放在变化分布下沿，留出集召回率仅 92.8%；修正为静止上界与变化 1% 分位的中点后复测通过。
- 留出集抓帧成功率：100%。
- 最大网格误差：2%。
- 变化点召回率：100%。
- 静止误报率：0%。
- 失败样本索引：无。
- 质量门：通过。

机器可读数据与详细样本索引：`capture-quality.json`；简版指标：`capture-quality.md`。

## Pikafish 现场结果

- 官方版本：`Pikafish 2026-01-02`。
- 引擎：`engines/Pikafish.2026-01-02/Windows/pikafish-bmi2.exe`。
- 引擎 SHA-256：`83429d412a9678d08ed374735db370869008f955cbb1af4d76df0a0ed853ca04`。
- NNUE SHA-256：`c4026370d7516d9b0f668447f9ca1931241538bdc689cde6fec6a991ac4d5f77`。
- 直接 UCI：收到 `uciok`、`readyok`，成功加载 64 MiB `pikafish.nnue`。
- MultiPV=3、深度 6：收到三条有效变化，`bestmove b2e2 ponder b9c7`。
- 真实 Engine Manager：100 次 `stop → ready → position → go` 均收到对应新版本的首条 `info`。
- 强制结束：实际终止子进程后恢复到 `ANALYZING`；连续终止三次重启进程后进入 `FAILED`。
- 故障注入发现 stdin `EPIPE`，已接管输入流错误并完成回归测试。

## 离线复核

- 使用 Windows 防火墙临时规则，只阻断 Pikafish BMI2 可执行文件的出站网络，不影响整机或 Codex 网络。
- 阻断生效后运行真实引擎集成测试：2/2 通过，包括 100 次屏障切换和强制结束恢复/三次失败降级。
- 测试退出码：0。
- `finally` 已删除临时防火墙规则，并复核规则不存在。
- 机器可读证据：`offline-pikafish.json`。

## 自动化回归

- `pnpm typecheck`：通过。
- Vitest：13 个测试文件、230 项测试全部通过。
- Renderer、Electron 主进程和 preload 生产构建：通过。

## 结论

按项目方确认的 100%/125%、200 事件范围，阶段 0 捕获质量门和阶段 1 本地 Pikafish 引擎现场验收通过。150% DPI 不在本次验收范围内。发布前仍建议人工通过应用文件选择器完整走一遍可视 UI。
